{
  "name": "@jurassic/jurassic",
  "version": "0.1.11",
  "license": "MIT",
  "tasks": {
    "build": "deno run -A --reload jsr:@jurassic/jurassic/export . && deno task runnbs && deno lint && deno fmt && deno task clean",
    "runnbs": "deno run --allow-read --allow-env --allow-run ./bin/runnbs.ts",
    "clean": "deno run --allow-read --allow-env --allow-write --allow-run ./bin/clean.ts",
    "docs": "deno run -A --reload ./bin/docs.ts . && deno lint && deno fmt",
    "docs:dev": "deno run -A --reload ./bin/docs.ts . && deno lint && deno fmt && cd _docs && pnpm install && pnpm run docs:dev"
  },
  "exports": {
    ".": "./mod.ts",
    "./clean": "./bin/clean.ts",
    "./docs": "./bin/docs.ts",
    "./export": "./bin/export.ts",
    "./runnbs": "./bin/runnbs.ts"
  },
  "imports": {
    "@std/fs": "jsr:@std/fs@^1.0.6",
    "typescript": "npm:typescript@^5.7.2",
    "zod": "npm:zod@^3.23.8",
    "jurassic/": "./jurassic/"
  },
  "lint": {
    "exclude": ["_docs", "docs"]
  }
}
